name: .NET

on:
  pull_request:
    branches:
      - main
    paths:
      - '**.fs'

jobs:
  call-setup:
    uses: ./.github/workflows/setup.yml
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Format
      run: npm run format:check
    - name: Test
      run: npm run test